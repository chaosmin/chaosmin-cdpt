<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="tech.chaosmin.framework.dao.UserDAO">
    <select id="pageExt" resultType="tech.chaosmin.framework.dao.dataobject.ext.UserExt">
        SELECT d.name as department, r.id as role_id, r.name as role, u.*
        FROM user u
        left join department d on d.id = u.department_id
        left join user_role m on m.user_id = u.id
        left join role r on r.id = m.role_id
        ${ew.customSqlSegment}
    </select>
</mapper> 
