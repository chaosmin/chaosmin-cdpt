<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="tech.chaosmin.framework.dao.UserDAO">
    <select id="pageExt" resultType="tech.chaosmin.framework.dao.dataobject.ext.UserExt">
        SELECT department.name as department, role.id as role_id, role.name as role, user.*
        FROM user
        left join department on department.id = user.department_id
        left join user_role on user_role.user_id = user.id
        left join role on role.id = user_role.role_id
        ${ew.customSqlSegment}
    </select>
</mapper> 
